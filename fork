为什么for()调用一次会有两次返回？
在不同的语言中，变量存储以及计算机参数和返回值的传输方式各不相同，这种差异被称为语言的调用约定。在X86体系结构里，
C语言的调用约定使用eax寄存器保存返回值和错误代码。fork()调用了sys_fork()系统调用,该系统调用所调用的copy_process函数
该函数会通过拷贝父进程来构造子进程，在构造子进程的TSS时，将子进程的eax设置为0，最后该函数返回子进程PID。fok系统调用
返回后，父进程中eax值为子进程PID，若子进程被调用则加载TSS,会使得eax为0，使得fork一次调用，两个返回值。
